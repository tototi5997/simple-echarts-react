import s from "./index.module.less"
import c from "classnames"

// echarts 组件列表
const EchartsList = () => {
  return <div className={c(s.echartsList)}>echarts list</div>
}

export default EchartsList
